{
  "name": "ghs3",
  "version": "1.0.0",
  "scripts": {
    "build": "cd backend && npm install && npm run build",
    "start": "cd backend && npm start"
  },
  "devDependencies": {
    "tailwindcss-animate": "^1.0.7"
  }
}
